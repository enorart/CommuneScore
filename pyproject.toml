[project]
name = "ideal-location"
version = "0.1.0"
description = "Ville Idéale IDF — ETL pipeline that produces a per-commune scoring dataset for Île-de-France"
requires-python = ">=3.13"
dependencies = [
    "pandas>=2.2",
    "geopandas>=1.0",
    "shapely>=2.0",
    "requests>=2.32",
    "pyarrow>=17.0",
    "polars>=1.43.0",
]
